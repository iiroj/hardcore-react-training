:global {
  @import 'css-wipe';

  html {
    font-family: 'Comic Sans Ms';
  }
}

.app {
  margin: 1em;
}

.header {
  align-items: center;
  display: flex;
  margin-bottom: 2em;

  img {
    height: 3em;
    margin-right: 1em;
  }

  button {
    margin-left: auto;
  }
}

.heading {
  font-size: 2em;
  font-weight: bold;
}
